        # ----- HEAD
        #       parse/S
        #       Parse a given expression list or expression.
        # ----- SOURCE
        #       Parse a list until there is no more
parseList:
        
        ret

